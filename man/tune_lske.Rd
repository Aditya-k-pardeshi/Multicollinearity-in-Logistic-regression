% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lske_logit.R
\name{tune_lske}
\alias{tune_lske}
\title{Tune Logistic Stein K-Liu Estimator (LSKE)}
\usage{
tune_lske(
  X,
  y,
  k_seq = seq(0, 5, by = 0.1),
  criterion = c("AIC", "accuracy"),
  verbose = TRUE
)
}
\arguments{
\item{X}{Matrix or data frame of predictors (without intercept)}

\item{y}{Binary response vector (0/1)}

\item{k_seq}{Sequence of k values to try (default: seq(0, 5, by = 0.1))}

\item{criterion}{Model selection criterion: "AIC" (default) or "accuracy"}

\item{verbose}{Logical; if TRUE, prints progress for each k.}
}
\value{
A list containing:
  - `best_model`: LSKE model with optimal k
  - `best_k`: selected k
  - `results`: data.frame of k and performance metric
}
\description{
Tunes the biasing parameter `k` in the Logistic Stein K-Liu Estimator (LSKE)
by searching over a grid and selecting the model with minimum AIC (default)
or highest accuracy if response `y` is given.
}
