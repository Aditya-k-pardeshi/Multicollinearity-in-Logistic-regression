% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lkle_logit.R
\name{tune_lkle}
\alias{tune_lkle}
\title{Tune the Logistic Kibria and Lukman Estimator (LKLE)}
\usage{
tune_lkle(
  X,
  y,
  k_seq = seq(0, 2, 0.2),
  K_seq = seq(0, 1, 0.1),
  criterion = c("AIC", "accuracy"),
  folds = 5
)
}
\arguments{
\item{X}{Matrix or data frame of predictors (without intercept)}

\item{y}{Binary response vector (0/1)}

\item{k_seq}{Sequence of ridge-type shrinkage values to test (default = seq(0, 2, 0.2))}

\item{K_seq}{Sequence of Liu-type adjustment values to test (default = seq(0, 1, 0.1))}

\item{criterion}{Criterion for tuning: "AIC" or "accuracy"}

\item{folds}{Number of folds for cross-validation if criterion = "accuracy" (default = 5)}
}
\value{
A list containing:
\itemize{
  \item `best_k` - best ridge shrinkage parameter
  \item `best_K` - best Liu adjustment parameter
  \item `best_value` - best AIC or accuracy achieved
  \item `results` - full tuning grid results
}
}
\description{
Performs a grid search over k and K parameters to find the best
(k, K) combination minimizing AIC or maximizing prediction accuracy.
}
