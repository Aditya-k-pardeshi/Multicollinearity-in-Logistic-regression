% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lkle_logit.R
\name{lkle_logit}
\alias{lkle_logit}
\title{Logistic Kibria and Lukman Estimator (LKLE)}
\usage{
lkle_logit(X, y, k = 0.1, K = 0.3, tol = 1e-06, max_iter = 50)
}
\arguments{
\item{X}{Predictor matrix or data frame (without intercept)}

\item{y}{Binary response vector (0 or 1)}

\item{k}{Ridge shrinkage parameter (k ≥ 0)}

\item{K}{Liu adjustment parameter (0 ≤ K ≤ 1)}

\item{tol}{Convergence tolerance (unused, for API consistency)}

\item{max_iter}{Iterations (unused, for API consistency)}
}
\value{
An object of class "lkle_logit"
}
\description{
Fits the Logistic Kibria and Lukman Estimator using:
\deqn{\hat{\beta}_{LKLE} = (X'WX + kI)^{-1}(X'WX + KI)\hat{\beta}_{MLE}}
}
\details{
This is a two-parameter modification of the logistic MLE, designed to handle
multicollinearity by combining ridge-type (k) and Liu-type (K) shrinkage.
The intercept is not penalized.
}
