% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lde_logit.R
\name{tune_lde}
\alias{tune_lde}
\title{Tune Logistic Dorugade Estimator (LDE)}
\usage{
tune_lde(
  X,
  y,
  K_seq = seq(0, 2, 0.2),
  d_seq = seq(0, 1, 0.1),
  criterion = c("AIC", "accuracy"),
  folds = 5
)
}
\arguments{
\item{X}{Matrix or data frame of predictors (without intercept)}

\item{y}{Binary response vector (0 or 1)}

\item{K_seq}{Sequence of ridge-type values to test (default = seq(0, 2, 0.2))}

\item{d_seq}{Sequence of Liu-type values to test (default = seq(0, 1, 0.1))}

\item{criterion}{Criterion for tuning: "AIC" or "accuracy"}

\item{folds}{Number of folds for cross-validation (if criterion = "accuracy")}
}
\value{
A list containing:
\itemize{
  \item `best_K` - best ridge-like shrinkage parameter
  \item `best_d` - best Liu-type biasing parameter
  \item `best_value` - best (lowest AIC or highest accuracy)
  \item `results` - full tuning grid
}
}
\description{
Performs a grid search over (K, d) parameters to find the best
combination minimizing AIC or maximizing accuracy.
}
